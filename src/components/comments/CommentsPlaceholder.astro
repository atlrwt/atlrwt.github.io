---
interface Props {
  enabled?: boolean;
}

const { enabled = false } = Astro.props;
---

{
  enabled && (
    <section aria-label="Comments">
      <p>Comments integration is reserved for a future decision.</p>
    </section>
  )
}
